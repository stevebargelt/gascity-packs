{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(*bd mol pour*patrol*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'BLOCKED: Patrol formulas must use wisps, not persistent molecules.' && echo 'Use: bd mol wisp <formula>' && echo 'Not: bd mol pour <formula>' && exit 2"
          }
        ]
      },
      {
        "matcher": "Bash(*bd mol pour *mol-witness*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'BLOCKED: Patrol formulas must use wisps, not persistent molecules.' && echo 'Use: bd mol wisp <formula>' && echo 'Not: bd mol pour <formula>' && exit 2"
          }
        ]
      },
      {
        "matcher": "Bash(*bd mol pour *mol-deacon*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'BLOCKED: Patrol formulas must use wisps, not persistent molecules.' && echo 'Use: bd mol wisp <formula>' && echo 'Not: bd mol pour <formula>' && exit 2"
          }
        ]
      },
      {
        "matcher": "Bash(*bd mol pour *mol-refinery*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'BLOCKED: Patrol formulas must use wisps, not persistent molecules.' && echo 'Use: bd mol wisp <formula>' && echo 'Not: bd mol pour <formula>' && exit 2"
          }
        ]
      }
    ]
  }
}
